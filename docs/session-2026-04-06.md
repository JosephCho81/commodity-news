# 세션 2 — 합금철 탭 프롬프트 전면 리팩토링 (2026-04-06)

## 발단
FeMn·SiMn 섹션에 "정보 부재", "최신 동향 미확보" 등 placeholder 텍스트 다수 노출.

**근본 원인**
1. 단일 `ferroalloy.js`에서 3품목 동시 요청 → FeSi가 토큰 선점, FeMn·SiMn 품질 저하
2. 검색 쿼리 FeSi 중심 → FeMn·SiMn 데이터 수집 미흡
3. placeholder 금지 규칙 부재

## 구조 변경

| 기존 (삭제) | 신규 | maxTokens |
|------------|------|-----------|
| `ferroalloy.js` | `fesi.js` | 2,500 |
| `ferrosilicon.js` (dead code) | `femn.js` | 2,000 |
| — | `simn.js` | 2,000 |
| — | `ferroalloy-summary.js` | 1,000 |

**호출 방식**: Step1 fesi/femn/simn 병렬 `Promise.allSettled` → Step2 summary 순차 호출
**에러 처리**: 개별 실패 시 `_latest` fallback, 전체 실패 시 latestData 반환

## 각 프롬프트 고유 스펙

| 파일 | 고유 필드 | 비중국 생산국 |
|------|----------|-------------|
| fesi.js | hbis_bid_price(null 금지), ningxia_spot, china_production_status | 노르웨이(Elkem), 러시아(ChEZ·RUSAL), 인도(IMFA·Shyam Ferro) |
| femn.js | hbis_bid_price(null 허용), mn_ore_cif_korea(USD 예외), ore_to_femn_spread | 카자흐스탄(TNC Kazchrome), 남아프리카(Samancor), 가봉(Eramet/Comilog) |
| simn.js | hbis_bid_price(null 허용), china_overcapacity_note, dual_input_cost | 말레이시아(OM Materials), 인도(Nava Bharat·FACOR), 남아프리카(Transalloys·Hernic) |
| ferroalloy-summary.js | intl_context, non_china_summary, outlook 3개만 생성 | — |

※ simn 3번째 나라: 카자흐스탄(FeMn과 중복) → 남아프리카로 교체

## 공통 규칙 강화

- placeholder 텍스트 금지 ("정보 부재", "최신 동향 미확보", "데이터 없음" 등)
- `key_issues`: 정확히 1개, 빈 배열 금지
- `non_china_producers`: 3개국 고정, 완전한 문장, 수치 포함
- `price_cny`: 숫자만, null 금지, 중간값 fallback 명시
- ferroalloy-summary.js 슬라이스: context 200자, supply_cause 150자

## UI 수정 (FerroalloyTab.tsx)
- FesiExtra / FemnExtra / SimnExtra 컴포넌트 신규 추가
- `SUMMARY_ROWS`에서 fesi/femn/simn 행 제거 (개별 카드와 중복)
- 합금철 시장 종합 섹션: 국제 정세 / 비중국 생산 / 단기 전망 3행만 표시

## 수정 파일
`api/_prompts/fesi.js` `femn.js` `simn.js` `ferroalloy-summary.js` (신규)
`api/_prompts/ferroalloy.js` `ferrosilicon.js` (삭제)
`api/get-news.js` `src/tabs/FerroalloyTab.tsx` `src/types.ts` (수정)
